/**
 * @deprecated Legacy seed — replaced by src/scripts/seed.ts for the swimming app.
 * This file is kept to avoid breaking existing imports; it is a no-op.
 */
export async function seedDemoUser(): Promise<void> {
  return;
}
